# Netlify build for the Next.js app. The plugin turns the App Router's routes
# into functions; nothing here is per-seller — each seller runs their own site
# from this same repository and sets their own environment variables.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
